
beforeEach(() => {

})

describe('getEvent', () => {
    test('gets event', () => {

    })
})

describe('createEvent', () => {
    test('creates event with uuid', () => {
        
    })
})

describe('syncEvents', () => {
    test('syncs event', () => {
        
    })

    test('makes multiple calls when pageToken is not null', () => {
        
    })
})

describe('getEvents', () => {
    test('gets related', () => {
        
    })
})

describe('relatedTEvents', () => {
    test('gets related', () => {
        
    })

    test('returns empty event')
})

describe('respondEvent', () => {
    test('responds to event', () => {
        
    })
})

describe('activateEvent', () => {
    test('activates event', () => {
        
    })

    test('checks if admin', () => {
        
    })
})

describe('userLocations', () => {
    test('uploads locations event', () => {
        
    })
})