
beforeEach(() => {

})

describe('getUsers', () => {
    test('gets users without sensitive info', () => {
        
    })

    test('filters', () => {
        
    })
})

describe('getUser', () => {
    test('gets user', () => {
        
    })
})

describe('createUser', () => {
    test('creates user if doesnt exist', () => {
        
    })

    test('does not create user if exist', () => {
        
    })
})

describe('invitedEvents', () => {
    test('retrieves successfully', () => {
        
    })
})

describe('updateLocation', () => {
    test('updates', () => {
        
    })
})